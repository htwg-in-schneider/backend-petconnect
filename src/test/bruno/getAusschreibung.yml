info:
  name: getAusschreibung
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8081/api/ausschreibungen/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5